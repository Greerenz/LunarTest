using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CounterAttack : MonoBehaviour
{
  /*  bool a;
    int b;
    private void OnTriggerEnter2D(Collider2D collision)
    {
        if(collision.gameObject.tag == "Bullet")
        {
      //    Debug.Log("In");
            a = true;
        }
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        if (collision.gameObject.tag == "Bullet")
        {
          //Debug.Log("Out");cx1cacaxc
            a = false;
        }
    }


    private void Update()
    {
        if (a)
        {
            if (Input.GetKeyDown(KeyCode.W))
            {
                GameObject[] Bullet = GameObject.FindGameObjectsWithTag("Bullet");
                    foreach (GameObject A in Bullet)
                    {
                    if (A.GetComponent<BulletDamage>().Ball == 0)
                    {
                        Bullet[0].GetComponent<BulletDamage>().Ball = 1;
                        a = false;
                       // Debug.Log("A");
                    }
                    else
                    {
                        Bullet[1].GetComponent<BulletDamage>().Ball = 1;
                        a = false;
                    }

                    }  
            }
        }
    }
  */
}
